spring:
  application:
    name: ConfigServer
  cloud:
    bus:
      trace:
        enabled: true
    config:
      server:
        git:
          uri: file:/C:/Users/Rico/Desktop/Spring Cloud/Solutions/config-repo

server:
  port: 8600

management:
  port: 9600
  security:
    enabled: false

#spring.sleuth.enabled: false
